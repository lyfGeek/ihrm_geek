server:
  port: 6868
eureka:
  client:
    register-with-eureka: false  # 是否将自己注册到 Eureka 服务中心。
    fetch-registry: false  # 是否从 Eureka 获取注册信息。
    service-url:  # Eureka 客户端与 Eureka 服务端交互地址。
      defaultZone: http://127.0.0.1:${server.port}/eureka/
